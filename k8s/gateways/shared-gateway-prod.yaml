apiVersion: gateway.networking.k8s.io/v1
kind: Gateway

metadata:
  name: shared-gateway-prod

spec:
  gatewayClassName: istio
  listeners:
    - name: http
      port: 8083
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              shared-gateway-prod-access: 'true'
